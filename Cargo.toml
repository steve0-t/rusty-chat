[package]
name = "chat_app"
version = "0.1.0"
edition = "2024"

[dependencies]
websocket = { version = "0.27.1", features = ["async", "async-ssl", "bytes", "futures", "native-tls", "sync", "sync-ssl"] }
